# Coluna de cameras — Quarto Casal
# Replicado do modelo base: room_living_cameras.yaml (Sala)
# camera_view: auto — tenta live primeiro, fallback para still image
- type: picture-entity
  # ANTERIOR: entity: camera.camera_quarto_casal (corrigido: cameras usam _2)
  entity: camera.camera_quarto_casal_2
  camera_view: auto
  aspect_ratio: 3:2
  show_name: false
  show_state: false
