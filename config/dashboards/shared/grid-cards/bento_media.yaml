- type: markdown
  content: |
    <small style="letter-spacing:.12em;opacity:.6;">MÍDIA</small>

    <br>

    🎵 **As It Was**

    Harry Styles · Sala

    ⏯ ▶⏭ TV Sala
  card_mod:
    style: |
      ha-card {
        height: 100%;
        border-radius: 24px;
        border: 1px solid rgba(92,131,223,0.42);
        background: linear-gradient(180deg, rgba(49,90,165,0.32) 0%, rgba(23,51,94,0.54) 100%);
        backdrop-filter: blur(34px) saturate(165%);
        -webkit-backdrop-filter: blur(34px) saturate(165%);
        box-shadow: inset 0 1px 0 rgba(255,255,255,0.30), inset 0 -1px 0 rgba(255,255,255,0.05), 0 18px 35px rgba(0,0,0,0.30);
        color: rgba(255,255,255,0.95);
      }
