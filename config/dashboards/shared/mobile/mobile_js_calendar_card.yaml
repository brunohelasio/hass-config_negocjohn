# NOVO JS - Mobile agenda frame.
# FALLBACK: trocar este include por mobile_calendar_card.yaml.
type: custom:bruno-mobile-card-frame
height: 190px
min_height: 190px
card:
  type: custom:bruno-agenda-card
