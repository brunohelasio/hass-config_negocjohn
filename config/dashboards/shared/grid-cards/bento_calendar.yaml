- type: markdown
  content: |
    <small style="letter-spacing:.12em;opacity:.58;">AGENDA</small>

    <br>

    15h00 · Reunião

    18h30 · Academia

    20h00 · Jantar

    21h30 · Lembrete
  card_mod:
    style: |
      ha-card {
        background: linear-gradient(160deg, rgba(255,255,255,0.12), rgba(255,255,255,0.04));
        backdrop-filter: blur(18px) saturate(1.2);
        -webkit-backdrop-filter: blur(18px) saturate(1.2);
        border-right: 1px solid rgba(255,255,255,0.08);
        border-radius: 0;
        border-top: none;
        border-bottom: none;
        border-left: none;
        box-shadow: none;
        height: 100%;
        overflow: hidden;
        aspect-ratio: unset;
        padding: 0;
      }
