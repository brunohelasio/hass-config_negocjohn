# Coluna de cameras — Quarto Marina
# Replicado do modelo base: room_living_cameras.yaml (Sala)
# camera_view: auto — tenta live primeiro, fallback para still image
- type: picture-entity
  # ANTERIOR: entity: camera.qma_camera (corrigido: cameras usam _2)
  entity: camera.qma_camera_2
  camera_view: auto
  aspect_ratio: 3:2
  show_name: false
  show_state: false
