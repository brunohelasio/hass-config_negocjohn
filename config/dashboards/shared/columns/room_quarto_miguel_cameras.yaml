# Coluna de cameras — Quarto Miguel
# Replicado do modelo base: room_living_cameras.yaml (Sala)
# camera_view: auto — tenta live primeiro, fallback para still image
- type: picture-entity
  entity: camera.qmi_camera
  camera_view: auto
  aspect_ratio: 3:2
  show_name: false
  show_state: false
