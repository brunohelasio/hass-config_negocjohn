- type: custom:layout-card
  layout_type: custom:grid-layout
  layout:
    grid-template-rows: auto 1fr auto
    height: 100%
    padding: 10px 0
  card_mod:
    style: |
      ha-card {
        --ios-blue: rgba(10, 132, 255, 0.3);
        --ios-blue-glow: rgba(10, 132, 255, 0.6);
        --glass-border: rgba(255, 255, 255, 0.2);
        height: 100%;
        border-radius: 24px;
        background: var(--ios-blue);
        backdrop-filter: blur(25px) saturate(180%);
        -webkit-backdrop-filter: blur(25px) saturate(180%);
        border: 1px solid var(--glass-border);
        border-top: 1px solid rgba(255, 255, 255, 0.4);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2), 0 0 25px var(--ios-blue-glow), inset 0 1px 2px rgba(255, 255, 255, 0.4);
      }
  cards:
    - type: vertical-stack
      cards:
        - &nav_icon
          type: custom:button-card
          icon: mdi:home-outline
          show_name: false
          tap_action: { action: none }
          hold_action: { action: none }
          styles:
            card: [width: 38px, height: 38px, margin: 3px auto, border-radius: 12px, background: transparent, border: none, box-shadow: none]
            icon: [color: rgba(255,255,255,0.88), width: 17px]
        - <<: *nav_icon
          icon: mdi:music-note-outline
        - <<: *nav_icon
          icon: mdi:shield-outline
        - <<: *nav_icon
          icon: mdi:radar
        - <<: *nav_icon
          icon: mdi:robot-vacuum
        - <<: *nav_icon
          icon: mdi:wifi
        - <<: *nav_icon
          icon: mdi:refresh
        - <<: *nav_icon
          icon: mdi:download
        - <<: *nav_icon
          icon: mdi:cube-outline
    - type: markdown
      content: '&nbsp;'
    - type: vertical-stack
      cards:
        - <<: *nav_icon
          icon: mdi:monitor-dashboard
        - <<: *nav_icon
          icon: mdi:power
