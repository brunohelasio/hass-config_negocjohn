type: custom:stack-in-card
view_layout:
  grid-area: media
card_mod:
  style: |
    :host {
      height: 100% !important;
    }

    ha-card {
      /* Mantém dimensões originais do bloco Bento */
      height: 100% !important;
      display: flex !important;
      flex-direction: column !important;
      justify-content: stretch !important;

      background: linear-gradient(160deg, rgba(96,165,250,0.22), rgba(59,130,246,0.08)) !important;
      border: 1px solid rgba(147,197,253,0.34) !important;
      border-radius: 18px !important;
      box-shadow: 0 0 32px rgba(59,130,246,0.12), inset 0 1px 0 rgba(191,219,254,0.15), 0 8px 32px rgba(0,0,0,0.2) !important;
      overflow: hidden !important;

      /* Bloco transparente, sem vidro e sem borda */
      backdrop-filter: blur(20px) saturate(1.35) !important;
      -webkit-backdrop-filter: blur(20px) saturate(1.35) !important;
      --ha-card-glass-tint: transparent !important;
      --ha-card-backdrop-filter: blur(20px) saturate(1.35) !important;
      --ha-card-glass-inset-shadow: none !important;
      --ha-card-background: linear-gradient(160deg, rgba(96,165,250,0.22), rgba(59,130,246,0.08)) !important;
      --ha-card-border-width: 0 !important;
    }

    ha-card::before {
      display: none !important;
    }
cards: !include ../../shared/grid-cards/bento_media.yaml
