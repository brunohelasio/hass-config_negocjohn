- type: markdown
  content: |
    <small style="letter-spacing:.12em;opacity:.6;">ROBOROCK</small>

    <br>

    ◯

    **Parado**

    Local: Sala

    Última: 10h45
  card_mod:
    style: |
      ha-card {
        height: 100%;
        border-radius: 24px;
        border: 1px solid rgba(167,139,250,0.45);
        background: linear-gradient(180deg, rgba(133,118,220,0.30) 0%, rgba(53,53,109,0.52) 100%);
        backdrop-filter: blur(34px) saturate(165%);
        -webkit-backdrop-filter: blur(34px) saturate(165%);
        box-shadow: inset 0 1px 0 rgba(255,255,255,0.30), inset 0 -1px 0 rgba(255,255,255,0.05), 0 18px 35px rgba(0,0,0,0.30);
        color: rgba(255,255,255,0.95);
      }
